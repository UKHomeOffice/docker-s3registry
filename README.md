# Docker registry using secrets

Currently generates S3 configuration from `/etc/secrets` using files:
```
/etc/secrets/s3-accesskey
/etc/secrets/s3-secretkey
/etc/secrets/s3-region
/etc/secrets/s3-bucket
```
